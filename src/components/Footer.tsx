import React from "react";
import styled from "styled-components";

const footerItems = [{}];

const Footer = () => {
  return (
    <FooterContainer>
      <footer className="border-2 border-solid border-gray-600">
        <h3>Get In Touch</h3>
        <div className="">
          <div>
            <ul className=""></ul>
          </div>
        </div>
      </footer>
    </FooterContainer>
  );
};

const FooterContainer = styled.div``;

export default Footer;
